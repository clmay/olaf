defmodule Olaf do
  def search(params) do
    {:ok, result} = {:ok, params}
    result
  end

  def parse(data) do
    {:ok, result} = {:ok, data}
    result
  end

  def format(data) do
    {:ok, result} = {:ok, data}
    result
  end
end
